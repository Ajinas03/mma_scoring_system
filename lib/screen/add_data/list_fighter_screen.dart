import 'package:flutter/material.dart';

class AddFighterScreen extends StatelessWidget {
  const AddFighterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
